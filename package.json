{
  "name": "smoothmyvideo",
  "version": "1.0.0",
  "description": "Desktop GUI for AI video frame interpolation (GMFSS), Python engine under the hood",
  "author": "Florin Nutiu",
  "main": "dist/main.js",
  "scripts": {
    "start": "tsc && electron .",
    "starti": "node -e \"const fs=require('fs'); fs.rmSync('node_modules', { recursive: true, force: true }); fs.rmSync('package-lock.json', { force: true })\" && npm install && npm start",
    "dist": "node -e \"require('fs').rmSync('release', { recursive: true, force: true })\" && tsc && electron-builder"
  },
  "devDependencies": {
    "@types/node": "latest",
    "electron": "latest",
    "electron-builder": "latest",
    "typescript": "latest"
  },
  "private": true,
  "build": {
    "appId": "com.smoothmyvideo.app",
    "productName": "SmoothMyVideo",
    "directories": { "output": "release" },
    "files": ["dist/**/*", "renderer/**/*", "icon.ico", "icon.png"],
    "extraResources": [
      {
        "from": "engine",
        "to": "engine",
        "filter": ["**/*", "!**/__pycache__/**", "!GMFSS_Fortuna/vid_out/**", "!.venv/**",
          "!trt_cache/**", "!*.onnx", "!*.onnx.data", "!*.engine",
          "!trt_poc.py", "!trt_poc_gmflow.py", "!trt_integration_test.py", "!trt_dynamic_test.py",
          "!rtxvideo/**"]
      }
    ],
    "win": { "target": "zip", "icon": "icon.ico" }
  }
}
